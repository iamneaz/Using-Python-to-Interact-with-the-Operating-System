#   REDIRECTION 
#   --  the process of sending a stream to a different destination
./stdout.py > stdout.txt
#   Each time we perform a redirection of STDOUT the destination is overwritten
#   to append we use a >> sign
./stdout.py >> stdout.txt

